# Cryptography Cracker

Vamos usar:
1. Python
    - Poetry
    - SQLAlchemy
    - Celery (RabbitMQ)
    - FastAPI
2. Docker
3. PostgresSQL

Rodar o código:
`sudo docker compose up --build`
use este comando na pasta principal do projeto
